TODO
- [x] Create header file
- [x] Use kPadding and struct Coordinate{size_t x, size_t y};
- [x] Set testcases to run automatically in GitHub actions
- [ ] Implement flags and options
    - [ ] Flag: -d --debug, does not create the animation
    - [ ] Options: -r --rows, -c --columns, -f --file, -g --generations
- [x] Optimize game, board is being allocated and freed in each generation
    - [x] Refactor game to allocate board only once
    - [ ] Figure out a way to optimize board parsing (can I do better than O(n * m))
---
